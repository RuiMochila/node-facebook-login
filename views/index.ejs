<% if (!user) { %>
	<h2>Welcome! Please log in.</h2>
<% } else { %>
	<h2>Hello, <%= user.username %>.</h2>
	<h2>Hello, <%= user.displayName %>.</h2>
	<!-- a pic entra no render -->
	<img src=<%= pic %> />
<% } %>
