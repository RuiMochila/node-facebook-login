<% if (!user) { %>
	<h2>Welcome! Please log in.</h2>
<% } else { %>
	<h2>Hello, <%= user.username %>.</h2>
	<h2>Hello, <%= user.displayName %>.</h2>
	<!-- a pic entra no render -->
	<img src=<%= pic %> />
<% } %>


<script>
	//pedido da lista de amigos
	var url = "/"+user.id+"/friends?fields=username,name";
	FB.api(url, function(response) {
  		//do comething with response
  		//a resposta vem no formato que tu viste no fb. 
  		//Agora... n sei onde meter este scrip nem como o chamar xD
	});
</script>